[package]
name = "t369-inference"
version = "0.1.0"
edition = "2021"
authors = ["SkyAInet Team"]
description = "T369Inference - Roman Neural Inference Engine (Pure Rust)"

[dependencies]
# Aucune dépendance externe pour l'inférence (objectif atteint)
# On utilisera plus tard rayon pour le parallélisme si besoin